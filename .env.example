# Local secrets — copy to ".env" in the repo root (gitignored):
#   cp .env.example .env
# Then load into your shell before running the app or scripts:
#   source scripts/source-env.sh
#
# Or run the app with env pre-loaded:
#   ./scripts/dev-server.sh

DEBUG_HEADERS_ACCESS_TOKEN=
